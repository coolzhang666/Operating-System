package top.coolzhang.main;

import top.coolzhang.scheduler.Scheduler;

public class Main {
    public static void main(String[] args) {
        Scheduler scheduler = new Scheduler();
        scheduler.start();
    }
}
